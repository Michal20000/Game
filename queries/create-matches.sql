CREATE TABLE matches
(
	id TEXT,
	host_id TEXT,
	guest_id TEXT,
	host_image INTEGER,
	guest_image INTEGER,
	host_rating INTEGER,
	guest_rating INTEGER,
	host_change INTEGER,
	guest_change INTEGER,
	match_date TEXT

);
